name: Automated Tests

on:
  push:
    branches:
      - implement-automated-tests-for-homepage-banner
      - main

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Install Dependencies
        run: npm install
      - name: Run Automated Tests
        run: npm test